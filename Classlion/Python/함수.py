#함수
#코드를 기능으로 묶은 단위, 코드의 기능 단위

def 함수이름(인자):
    실행코드
    리턴값

#인스타그램 만들기
#복잡하고 긴 코드를 기능별로 묶어서, 간단하고 접목하기 쉽게 만드는 것(정돈하는 것)
#Divide and Conquer D.A.C

#input = 인자, 개수는 마음대로, 없어도 됨
#output = return 값, 리턴값은 하나 또는 없을 수도 있음 
#변수의 범위
# 함수 안에서는 어떤 변수를 선언하든 상관없음, 영향받지 않음, 독립적, 박에 있는 함수랑 이름 같아도 됨
# 함수 안의 변수 : 지역변수 local 변수
# vs. 전역변수 global - 이건 가급적 쓰지 않는다
# WHY? 기능별로 영역구분이 안되니까
